micronaut:
  security:
    token:
      jwt:
        signatures:
          secret:
            generator:
              secret: pleaseChangeThisSecretForANewOne
              jws-algorithm: HS256
        generator:
          refresh-token:
            secret: pleaseChangeThisSecretForANewOneRefresh
---
permissions-manager:
  application:
    passphrase:
      salt: "changeme"